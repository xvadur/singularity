# dependencies
node_modules/

# build output
dist/
.vite/
.cache/

# logs
*.log

# env
.env
.env.*
!.env.example

# OS
.DS_Store

# local archival (kept out of git because of quarantine/old deps)
legacy/

# keep the old UI around locally, but don't track it
apps/chatui/chatui-legacy-*/

# local multi-agent worktrees
.worktrees/
